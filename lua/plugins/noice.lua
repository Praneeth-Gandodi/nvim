return {
  {
    "folke/noice.nvim",
    enabled = false,
  },
}

-- Enable this below if you want noice

-- return {
--   "folke/noice.nvim",
--   opts = {
--     cmdline = {
--       enabled = false, -- back to normal command line at the bottom
--     },
--     messages = {
--       enabled = false, -- optional: also removes floating messages
--     },
--   },
-- }
